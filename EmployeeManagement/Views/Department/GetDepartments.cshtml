@model  IEnumerable<EmployeeManagement.Models.Department>

<div class="row">
    <div class="col-sm-3"></div>
    <div class="col-sm-6">
        <table class="table table-striped table-bordered">
            <thead>
                <tr class="text-center">
                    <th>Department Id</th>
                    <th>Department Name</th>
                    <th>Update</th>
                    <th>Delete</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var d in Model)
                {
                    <tr class="text-center">
                        <td>@d.DepartmentId</td>
                        <td>@d.DepartmentName</td>
                        <td><a asp-controller="Department" asp-action="Update" asp-route-DepartmentId="@d.DepartmentId" class="btn btn-outline-primary">Update</a></td>
                        <td><a asp-controller="Department" asp-action="Delete" asp-route-DepartmentId="@d.DepartmentId" class="btn btn-outline-danger">Delete</a></td>
                    </tr>
                }
            </tbody>
        </table>
        <a asp-controller="Department" asp-action="Create"  class="btn btn-outline-success">Create</a>
    </div>
    <div class="col-sm-3"></div>
</div>